const fs = require('fs'); const path = require('path'); const screens = require('../screens.cjs'); const root = path.resolve(__dirname,'..');
const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
function nav(role){if(role==='école')return '<a href="wf-v2-19-dashboard-ecole.html">Tableau de bord</a><a href="wf-v2-20-mes-annonces.html">Mes annonces</a><a href="wf-v2-25-mon-compte.html">Mon compte</a><span>Déconnexion</span>';if(role==='admin')return '<a href="wf-v2-27-dashboard-admin.html">Dashboard</a><a href="wf-v2-28-admin-ecoles.html">Écoles</a><a href="wf-v2-29-admin-annonces.html">Annonces</a><span>Déconnexion</span>';if(role==='candidat')return '<a href="wf-v2-14-suivi-attente.html">Suivi candidat</a>';return '<a href="wf-v2-01-accueil.html">Accueil</a><a href="wf-v2-02-annonces.html">Annonces</a><a href="wf-v2-05-alertes.html">Alertes</a><a href="wf-v2-09-connexion.html">Connexion</a><a href="wf-v2-10-inscription.html">Inscription auto-école</a>';}
function renderScreen(s){return `<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(s.title)} — wireframe v2</title><link rel="stylesheet" href="wireframe-v2.css"></head><body><div class="wf-ribbon">WIREFRAME V2 — ${esc(s.title)}</div><header><a class="brand" href="index.html">MoniteurConnect</a><nav>${nav(s.role)}</nav></header><main class="wf-page">${s.body}</main><aside class="wf-meta"><b>${esc(s.filename.replace('.html','').toUpperCase())}</b><span>${esc(s.route)}</span><span>${esc(s.view)}</span><span>${esc(s.role)} · ${esc(s.state)}</span></aside><footer>MoniteurConnect — maquette fonctionnelle v2 · <a href="index.html">sommaire</a></footer></body></html>`;}
function renderIndex(){const groups=[...new Set(screens.map(s=>s.section))];return `<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Wireframes v2 — sommaire</title><link rel="stylesheet" href="wireframe-v2.css"></head><body><div class="wf-ribbon">WIREFRAMES V2 — SOMMAIRE</div><header><span class="brand">MoniteurConnect</span></header><main class="wf-page"><section class="hero"><h1>Maquette fonctionnelle v2</h1><p>30 écrans et états conformes à l'application livrée.</p></section>${groups.map(g=>`<section><h2>${g}</h2><div class="index-grid">${screens.filter(s=>s.section===g).map(s=>`<a class="card" href="${s.filename}"><b>${s.title}</b><small>${s.route}</small></a>`).join('')}</div></section>`).join('')}</main></body></html>`;}
function writeAll(){fs.writeFileSync(path.join(root,'index.html'),renderIndex());for(const s of screens)fs.writeFileSync(path.join(root,s.filename),renderScreen(s));}
if(require.main===module)writeAll(); module.exports={renderScreen,renderIndex,writeAll};
